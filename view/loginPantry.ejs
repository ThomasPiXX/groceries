<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title></title>
    <meta name="description" content="">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css">
    <style>
        body {
          display: flex;
          justify-content: center;
          align-items: center;
          height: 50vh;
        }
        .my-pantry-button {
            margin-right: 5px; /* Adjust the spacing between buttons */
        }
        .pantry-button-container {
            display: flex;
            justify-content: space-between; /* Align buttons with space between them */
            align-items: center; /* Center buttons vertically */
        }
        .pantry-button-container form {
            margin: 0; /* Remove default form margins */
        }
    </style>
</head>
<body>
    <div class="container">
        <h2>Login Form</h2>
        <div class="row">
            <div class="col s12 m6">
                <form action="/login" method="post">
                    <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                    <div class="input-field">
                        <input type="text" id="username" name="username" required placeholder="Username">
                    </div>
                    <div class="input-field">
                        <input type="password" id="password" name="password" required placeholder="Password">
                    </div>
                    <% if (typeof message !== 'undefined' && message !== null) { %>
                        <p style="color: red;"><%= message %></p>
                    <% } %>
                    <div class="pantry-button-container">
                        <button class="btn waves-effect waves-light my-pantry-button" type="submit">Login</button>
                        <form id="new-user-form" action="/createAccount" method="get">
                            <button class="btn waves-effect waves-light my-pantry-button" type="button" onclick="createAccount()">new user</button>
                        </form>
                        <form action="/" method="get">
                            <button class="btn waves-effect waves-light my-pantry-button" type="submit">home</button>
                        </form>
                    </div>
                </form>
            </div>
        </div>
    </div>
    <script src="groceries/materialize.js"></script>
    <script>
        function createAccount() {
            document.getElementById("new-user-form").submit();
        }
    </script>
</body>
</html>
